using UnityEngine;

namespace Oloxo.Core {

    /// <summary>
    /// Single point to bridge between application audio and system speakers.
    /// Also handles sound pooling and effects.
    /// </summary>
    public class AudioHandler : MonoBehaviour {
    }
}